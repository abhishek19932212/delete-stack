pipeline {
    agent master
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation delete-stack --stack-name jenkins-node2-stack --region 'us-west-2'"
              }
             }
            }
            }
